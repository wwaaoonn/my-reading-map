# 埋め込みモデル以外の依存。requirements.txt と requirements-dev.txt が読み込む
# 動作を確認した環境：Python 3.12.2 / macOS
pandas==2.2.1
numpy==1.26.4
scikit-learn==1.5.1
matplotlib==3.8.3
matplotlib-fontja==1.0.0   # 図の日本語表示
Janome==0.5.0              # 頻出語抽出の形態素解析
adjustText==1.3.0          # ラベルの重なりを自動でずらす
anthropic==1.7.0           # クラスタ名の生成（APIキーが無い場合は頻出語で代替）
Brotli>=1.2.0              # anthropic が使うhttpx2の要求。1.1.0以下だとAPI応答の解凍に失敗する
python-dotenv==1.0.1       # .env からAPIキーを読み込む
